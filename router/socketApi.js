const controls = require("controllers/index");
const config = require("config/config");

// 连接
exports.connect = (socket) => {
    // console.log(socket);
    // socket.on("ngChat",() => {

    // })
}

// 获取所有rooms
exports.getRooms = (data,socket) => {

}